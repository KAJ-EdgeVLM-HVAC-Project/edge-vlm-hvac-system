---
marp: true
theme: default
paginate: true
style: |
  @import url('https://fonts.googleapis.com/css2?family=Noto+Sans+KR:wght@400;700;900&display=swap');

  * { box-sizing: border-box; }

  section {
    font-family: 'Apple SD Gothic Neo', 'Noto Sans KR', 'Malgun Gothic', sans-serif;
    background: #f8f9fc;
    color: #1a1a2e;
    padding: 32px 52px;
    font-size: 14.5px;
  }

  h1 { font-size: 1.9em; color: #111838; line-height: 1.25; margin-bottom: 6px; }
  h2 { font-size: 1.4em; color: #111838; border-bottom: 3px solid #3a7bd5;
       padding-bottom: 8px; margin-bottom: 18px; }
  h3 { font-size: 1.02em; color: #3a7bd5; margin: 0 0 6px 0; }
  h4 { font-size: 0.88em; color: #555; margin: 0 0 4px 0; }
  strong { color: #c0392b; }
  em { color: #2980b9; font-style: normal; font-weight: bold; }
  code { background: #eef2f7; border-radius: 4px; padding: 1px 6px;
         font-size: 0.87em; color: #2c3e50; font-family: 'Fira Code', monospace; }
  pre {
    background: #1e2433;
    color: #e8eaf0;
    border-radius: 8px;
    padding: 13px 17px;
    font-size: 0.77em;
    margin: 7px 0;
    line-height: 1.55;
  }
  pre code { background: none; color: inherit; padding: 0; }

  .two   { display: grid; grid-template-columns: 1fr 1fr; gap: 18px; }
  .three { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 14px; }
  .two-r { display: grid; grid-template-columns: 1.3fr 0.7fr; gap: 18px; }
  .two-l { display: grid; grid-template-columns: 0.7fr 1.3fr; gap: 18px; }
  .four  { display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; gap: 12px; }

  .card {
    background: #fff;
    border-radius: 10px;
    border: 1px solid #dde3ef;
    padding: 14px 16px;
  }
  .card-blue   { border-left: 4px solid #3a7bd5; }
  .card-red    { border-left: 4px solid #c0392b; background: #fdf5f5; }
  .card-green  { border-left: 4px solid #27ae60; background: #f3fdf6; }
  .card-orange { border-left: 4px solid #e67e22; background: #fdf9f3; }
  .card-purple { border-left: 4px solid #8e44ad; background: #faf5ff; }
  .card-teal   { border-left: 4px solid #16a085; background: #f0faf8; }
  .card-dark   { background: #1e2433; color: #e8eaf0; border: none; }
  .card-yellow { border-left: 4px solid #f39c12; background: #fffbf0; }
  .card-gray   { border-left: 4px solid #7f8c8d; background: #f7f9fa; }

  .pill {
    display: inline-block; padding: 3px 11px; border-radius: 20px;
    font-size: 0.77em; font-weight: bold; margin: 2px 2px;
  }
  .pill-blue   { background:#dbeafe; color:#1d4ed8; }
  .pill-green  { background:#d1fae5; color:#065f46; }
  .pill-red    { background:#fee2e2; color:#991b1b; }
  .pill-orange { background:#ffedd5; color:#9a3412; }
  .pill-teal   { background:#d1f0eb; color:#0f766e; }
  .pill-gray   { background:#f1f5f9; color:#475569; }
  .pill-purple { background:#ede9fe; color:#5b21b6; }
  .pill-yellow { background:#fef9c3; color:#713f12; }

  .badge {
    display: inline-block; background: #3a7bd5; color: #fff;
    border-radius: 6px; padding: 1px 8px; font-size: 0.74em;
    font-weight: bold; margin-right: 5px;
  }
  .badge-orange { background: #e67e22; }
  .badge-green  { background: #27ae60; }
  .badge-red    { background: #c0392b; }
  .badge-purple { background: #8e44ad; }
  .badge-teal   { background: #16a085; }
  .badge-gray   { background: #7f8c8d; }

  table { width:100%; border-collapse:collapse; font-size:0.83em; }
  th { background:#111838; color:#fff; padding:7px 11px; text-align:left; }
  td { border-bottom:1px solid #e2e8f0; padding:7px 11px; }
  tr:nth-child(even) td { background:#f8f9fc; }

  .note   { font-size:0.73em; color:#94a3b8; margin-top:5px; }
  .center { text-align:center; }
  .arrow  { color:#3a7bd5; font-size:1.3em; font-weight:bold; }
  .num    { font-size:2.0em; font-weight:900; color:#3a7bd5; line-height:1; }
  .num-green { font-size:2.0em; font-weight:900; color:#27ae60; line-height:1; }
  .num-red   { font-size:2.0em; font-weight:900; color:#c0392b; line-height:1; }
  .num-orange { font-size:2.0em; font-weight:900; color:#e67e22; line-height:1; }

  .cite {
    font-size: 0.72em; color: #7f8c8d; border-top: 1px solid #e2e8f0;
    margin-top: 10px; padding-top: 7px; line-height: 1.5;
  }

  section.title {
    background: linear-gradient(140deg, #0f1629 0%, #1a3a6e 60%, #0f2d55 100%);
    color: #fff;
    display: flex; flex-direction: column;
    justify-content: center; align-items: flex-start;
    padding: 52px 68px;
  }
  section.title h1 { color:#fff; font-size:1.95em; line-height:1.3; }
  section.title .en { color:#7fa8d8; font-size:0.82em; margin-top:6px; }
  section.title .team { color:#a8c4e0; font-size:0.8em; margin-top:26px; line-height:1.9; }
  section.title .tag { background:rgba(255,255,255,0.12); border-radius:6px;
                       padding:3px 12px; font-size:0.78em; color:#c8ddf0; margin-right:8px; }
  section.title .since { color:#5d9fd8; font-size:0.72em; margin-top:10px; }

  section.divider {
    background: linear-gradient(120deg, #1a2a4e 0%, #2a4a8e 100%);
    color: #fff;
    display: flex; flex-direction: column;
    justify-content: center; align-items: flex-start;
    padding: 52px 68px;
  }
  section.divider h1 { color:#fff; font-size:1.7em; line-height:1.3; }
  section.divider .num-big { font-size:5em; font-weight:900; color:rgba(255,255,255,0.08);
                              position:absolute; right:68px; top:50%; transform:translateY(-50%); }
  section.divider .sub { color:#8ab4e8; font-size:0.88em; margin-top:14px; line-height:1.7; }

  section.closing {
    background: linear-gradient(140deg, #0f1629 0%, #1a3a6e 100%);
    color: #fff;
    display: flex; flex-direction: column;
    justify-content: center; align-items: center; text-align: center;
  }
  section.closing h2 { color:#fff; border-bottom:2px solid rgba(255,255,255,0.3); width:100%; }
---

<!-- _class: title -->

# 엣지 VLM·IoT 융합 기반<br>온디바이스 AI 추론<br>지능형 공조 제어 시스템

<p class="en">Edge VLM-Driven On-Device AI Intelligent HVAC Control System</p>
<p class="en" style="margin-top:2px; color:#5d9fd8;">— 기말 발표 —</p>

<div class="team">
<span class="tag">VLM / AI 추론</span> 김준경 &nbsp;·&nbsp;
<span class="tag">PM / 시스템 통합</span> 김철호<br>
<span class="tag">열환경 모델링</span> 김민서 &nbsp;·&nbsp;
<span class="tag">데이터 분석</span> 정윤찬
</div>

<p class="since">동아대학교 컴퓨터공학과 &nbsp;|&nbsp; 2026. 06</p>

---

## 목차

<div class="two">
<div>

<div class="card card-blue" style="margin-bottom:12px;">

### Part 1. 중간발표 → 기말발표: 무엇이 달라졌나
- 전체 변경 요약
- Jetson Orin Nano Super 실배포
- llama.cpp INT4 CUDA 양자화 백엔드
- 신규 기능 (센서 · 영상분석 · UI)

</div>

<div class="card card-purple">

### Part 2. 핵심 구현 상세
- VLM 추론 파이프라인 및 파싱 방어 로직
- RC 열회로 기반 물리 시뮬레이션
- ISO 7730 PMV 계산 구현
- PID 제어 + 히스테리시스 + 5단계 상태 머신

</div>

</div>
<div>

<div class="card card-green" style="margin-bottom:12px;">

### Part 3. 검증 실험 — Edinburgh Dataset
- 데이터셋 소개 및 실험 설계
- AI 제어 vs 규칙기반 비교 결과
- PMV · 에너지 · 쾌적도 분석

</div>

<div class="card card-orange">

### Part 4. 시스템 이점 및 향후 계획
- 에너지 절감 · 쾌적도 향상 · 보안 우위
- 논문 작성 계획
- 실증 배포 로드맵

</div>

</div>
</div>

---

<!-- _class: divider -->

<span class="num-big">1</span>

# Part 1
# 중간발표 → 기말발표
# 무엇이 달라졌나

<p class="sub">
Jetson 실배포 완료 · llama.cpp INT4 CUDA 양자화<br>
SHT31 실물 센서 · 영상 파일 분석 모드 · UI 전면 개선
</p>

---

## 중간발표 이후 전체 변경 요약

<div class="card card-dark" style="padding:16px 22px; margin-bottom:16px;">

```
                 중간발표 (4월)                       기말발표 (6월)
────────────────────────────────────────────────────────────────────────────
하드웨어    Mac 개발 환경만                  ✅  Jetson Orin Nano Super 실배포 완료
VLM 백엔드  HuggingFace FP32 CPU (~60s)     ✅  llama.cpp INT4 CUDA (~15s on GPU)
온습도 입력 기상청 API 수치만               ✅  SHT31 I2C 실물 센서 (±0.3°C / ±2%RH)
카메라      USB 카메라만                    ✅  Jetson CSI GStreamer 파이프라인 추가
영상 분석   없음                            ✅  영상 파일 분석 모드 + 자동 리포트
물리 모델   단순 비율 온도 변화              ✅  RC 열회로 τ=3600s (ISO 기반 물리)
알고리즘    AI만 PMV 계산                   ✅  AI/RB 공정 비교 (동일 met/clo 기준)
UI          단순 시작화면                   ✅  환경 프로파일 선택 + VLM 컨텍스트 창
```

</div>

<div class="four">
<div class="card card-green" style="text-align:center; padding:10px;">
<div class="num-green">8</div>
<div style="font-size:0.82em; margin-top:3px;">신규 기능</div>
</div>
<div class="card card-blue" style="text-align:center; padding:10px;">
<div class="num">3</div>
<div style="font-size:0.82em; margin-top:3px;">알고리즘 고도화</div>
</div>
<div class="card card-orange" style="text-align:center; padding:10px;">
<div class="num-orange">2</div>
<div style="font-size:0.82em; margin-top:3px;">하드웨어 추가</div>
</div>
<div class="card card-purple" style="text-align:center; padding:10px;">
<div class="num" style="color:#8e44ad;">0</div>
<div style="font-size:0.82em; margin-top:3px;">미완료 항목</div>
</div>
</div>

---

## Jetson Orin Nano Super 실배포 — 배포 환경 및 해결 이슈

<div class="two">
<div>

<div class="card card-blue">

### 배포 환경

| 항목 | 사양 |
|------|------|
| 보드 | Jetson Orin Nano Super 8GB |
| OS | Ubuntu 22.04 (JetPack 6.x) |
| CUDA | 12.6 / Ampere arch 87 |
| GPU | 1024-core, 67 TOPS |
| RAM | 8 GB LPDDR5 (CPU·GPU 공유) |
| 배포 주소 | 172.20.10.11 (jetson) |

</div>

<div class="card card-teal" style="margin-top:12px;">

### 자동 실행 스크립트 `run.sh`

```bash
# DISPLAY 자동 감지 (SSH 환경 대응)
[ -z "$DISPLAY" ] && \
  export DISPLAY=:1 && \
  export XAUTHORITY=/run/user/1000/gdm/Xauthority

export GGML_CUDA_NO_VMM=1   # Jetson 통합메모리 OOM 방지
export PYTHONUNBUFFERED=1
python3 main.py "$@"
```

</div>

</div>
<div>

<div class="card card-red">

### 배포 과정에서 해결한 이슈 4가지

**① CUDA OOM (Jetson 통합 메모리)**
`GGML_CUDA_NO_VMM=1` — VMM 사용 억제, GPU 메모리 직접 할당

**② cv2 Qt 백엔드 충돌**
PyPI opencv Qt 백엔드 → Jetson GTK 시스템 opencv로 교체
```bash
ln -sfn /usr/lib/python3/dist-packages/cv2.*.so \
    ~/edge-vlm-hvac-system/vlm-env/lib/.../cv2.so
```

**③ pynput macOS 26 HIToolbox 크래시**
```python
_PYNPUT_OK = platform.system() != "Darwin"
# Mac: cv2.waitKey() fallback으로 대체
```

**④ YOLO cuDNN 추론 엔진 충돌**
`device='cpu'` 강제 — Jetson cuDNN 엔진 버전 불일치 회피

</div>

</div>
</div>

---

## llama.cpp INT4 CUDA 양자화 백엔드 — 핵심 구현

<div class="two">
<div>

<div class="card card-purple">

### Qwen2-VL-2B Q4_K_M 양자화

| 항목 | FP32 (HuggingFace) | **INT4 (llama.cpp)** |
|------|-------------------|---------------------|
| 모델 크기 | ~8 GB | **941 MB** |
| 비전 인코더 | ~2 GB | **1.3 GB (FP16)** |
| Jetson RAM | OOM 초과 | **~2.5 GB** |
| 추론 속도 | 불가 | **~15 s (GPU)** |
| 정확도 손실 | 기준 | **≈ 1% 이하** |

`Q4_K_M` — K-quantization Medium: 가중치 핵심 레이어를 부분 8비트 보존, 나머지 4비트 양자화 → 정확도·속도 균형 최적

</div>

<div class="card card-dark" style="margin-top:12px; padding:13px 15px;">

```bash
# Jetson CUDA arch 87 빌드
cmake -B build -DGGML_CUDA=ON \
  -DGGML_CUDA_FA=ON \
  -DCMAKE_CUDA_ARCHITECTURES=87 \
  -DCUDAToolkit_ROOT=/usr/local/cuda-12.6
cmake --build build -j4
# → llama-mtmd-cli (멀티모달 CLI)
```

</div>

</div>
<div>

<div class="card card-orange">

### VLM 백엔드 자동 선택 구조

```python
# vlm_processor.py  _init_backend()

PRIORITY = ['lcpp', 'trt', 'mps', 'cuda', 'cpu']

def _init_backend(self):
    # 1순위: llama.cpp CUDA INT4
    if Path('~/llama.cpp/build/bin/llama-mtmd-cli'
            ).expanduser().exists():
        self.backend = 'lcpp'
        return

    # 2순위: TensorRT FP16
    if Path('./qwen2vl_engine/').exists():
        self.backend = 'trt'
        return

    # 3순위: Apple Silicon MPS
    if sys.platform == 'darwin' and \
       torch.backends.mps.is_available():
        self.backend = 'mps'
        return

    # 4순위 / 5순위: CUDA / CPU
    self.backend = 'cuda' if torch.cuda.is_available() \
                          else 'cpu'
```

</div>

<div class="note" style="margin-top:6px;">개발(Mac MPS) → 배포(Jetson lcpp) 환경 전환 시 코드 수정 없이 자동 최적 백엔드 선택</div>

</div>
</div>

---

## 신규 기능 — SHT31 센서 · CSI 카메라 · 영상 분석 모드

<div class="three">

<div class="card card-teal">

### SHT31 I2C 온습도 센서

```python
# sensor_interface.py
# Jetson GPIO: Pin3(SDA) / Pin5(SCL)
# I2C Bus 7, Address 0x44

bus.write_i2c_block_data(
    0x44, 0x2C, [0x06])  # 고정밀 1회
time.sleep(0.5)
data = bus.read_i2c_block_data(0x44, 0, 6)

temp = -45 + 175 * \
    (data[0]<<8 | data[1]) / 65535
rh   = 100  * \
    (data[3]<<8 | data[4]) / 65535
```

**정밀도:** ±0.3°C / ±2% RH
센서 연결 시 자동 사용,
없으면 물리 시뮬레이션으로 fallback

</div>

<div class="card card-orange">

### CSI 카메라 GStreamer

```python
# Jetson 전용 파이프라인
GST = (
  "nvarguscamerasrc ! "
  "video/x-raw(memory:NVMM),"
  "width=1920,height=1080 ! "
  "nvvidconv flip-method=2 ! "
  # ↑ 물리 장착 방향 180° 보정
  "video/x-raw,format=BGRx ! "
  "videoconvert ! "
  "video/x-raw,format=BGR ! "
  "appsink"
)
cap = cv2.VideoCapture(
    GST, cv2.CAP_GSTREAMER)
```

USB(Mac) / CSI(Jetson) 자동 분기

</div>

<div class="card card-blue">

### 영상 파일 분석 모드

```
영상 파일 (MP4/AVI)
       ↓ 30초 단위 프레임 점프
  VLM 분석 (1회/구간)
       ↓ dt=0.1s × 300스텝
  물리 시뮬레이션
  (AI 제어 / RB 동시 진행)
       ↓
  자동 리포트 생성
```

```
results/분석명_날짜시간/
├── 01_pmv_comparison.png
├── 02_indoor_temp.png
├── 03_energy_cumulative.png
├── 04_energy_bar.png
├── 05_comfort_rate.png
├── analysis_log.csv
└── summary.txt
```

</div>

</div>

---

<!-- _class: divider -->

<span class="num-big">2</span>

# Part 2
# 핵심 구현 상세

<p class="sub">
VLM 추론 파이프라인 · RC 열회로 물리 시뮬레이션<br>
ISO 7730 PMV 구현 · PID + 히스테리시스 + 5단계 상태 머신
</p>

---

## 구현 ① — VLM 추론 파이프라인 및 3단계 파싱 방어

<div class="two">
<div>

<div class="card card-dark" style="padding:13px 15px;">

```python
# vlm_processor.py  _clean() — 3단계 필터

def _clean(text: str) -> str:
    lines = []
    for line in text.splitlines():
        l = line.strip()
        if not l: continue

        # ① ggml 내부 로그 접두어 제거
        if any(l.startswith(p) for p in (
            'E ggml','W ggml','I ggml',
            'llama_','clip_','load_tensors')):
            continue

        # ② 타이밍 줄 제거 (숫자로 시작)
        #    예: "0.00 0.02 0.015 E ggml_cuda_init..."
        if re.match(r'^\d+[\.\d]*\s', l):
            continue

        # ③ CUDA 에러 키워드 줄 제거
        if any(kw in l for kw in (
            'ggml_cuda_init',
            'failed to initialize CUDA',
            'CUDA-capable device','no CUDA')):
            continue
        lines.append(l)
    return ' '.join(lines).strip()
```

</div>

</div>
<div>

<div class="card card-orange">

### 파싱 실패 원인 및 해결

```
lcpp stdout 실제 오염 예시:
──────────────────────────────────────
0.00 0.02 E ggml_cuda_init:
  failed to initialize CUDA
{"sleeves":"long","activity":"sitting"...
──────────────────────────────────────
→ 타이밍줄 + CUDA 에러가 JSON 앞에 섞임
→ re.search('{.*}')가 오염 문자열 매칭
→ JSON 파싱 100% 실패 (기본값만 출력)
```

</div>

<div class="card card-green" style="margin-top:12px;">

### Regex Fallback — JSON 실패 시 개별 추출

```python
# _parse_response() — JSON 파싱 실패 시
data = {}
for key, pat in [
  ('sleeves',    r'"sleeves"\s*:\s*"(long|short)"'),
  ('activity',   r'"activity"\s*:\s*"(sitting|'
                 r'standing|walking|exercising)"'),
  ('outerwear',  r'"outerwear"\s*:\s*"(yes|no)"'),
  ('room_size',  r'"room_size"\s*:\s*"(small|'
                 r'medium|large)"'),
  ('heat_source',r'"heat_source"\s*:\s*"(yes|no)"'),
]:
    m = re.search(pat, raw_response)
    if m: data[key] = m.group(1)
# → 부분 파싱이라도 최대한 활용
```

</div>

</div>
</div>

---

## 구현 ② — VLM 출력 → PMV 입력값 변환 체계

<div class="two">
<div>

<div class="card card-dark" style="padding:13px 15px;">

```python
# vlm_processor.py  _parse_response() 반환값

# ① CLO 계산 (착의량)
CLO_BASE = {'long': 1.0, 'short': 0.5}
clo = CLO_BASE[data.get('sleeves', 'long')]
if data.get('outerwear') == 'yes':
    clo += 0.3   # 아우터: +0.3 clo

# ② MET 계산 (대사율)
MET_MAP = {
    'lying': 1.0, 'sitting': 1.0,
    'standing': 1.2, 'walking': 1.5,
    'cooking': 2.0, 'exercising': 3.0,
}
met = MET_MAP.get(data.get('activity'), 1.0)

# ③ 방 크기 → 물리 시뮬 size_factor
ROOM_MAP = {'small':15, 'medium':30, 'large':60}
room_m2 = ROOM_MAP.get(data.get('room_size'), 30)

# ④ 열원 → 복사온도(tr) 보정
TR_HEAT_OFFSET = 4.0   # 조리기구 감지 시 +4°C
tr = indoor_temp + (TR_HEAT_OFFSET
     if data.get('heat_source') == 'yes' else 0)
```

</div>

</div>
<div>

<div class="card card-blue">

### MET 결정 우선순위 (3계층)

```python
# main.py  — 매 5초 제어 루프

# 1순위: MotionDetector (실시간 물리)
#   → 운동·보행 등 빠른 변화 즉시 반영
if motion_score >= 15.0:
    met = 3.0   # 격렬한 운동
elif motion_score >= 5.0:
    met = 1.5   # 보행 수준

# 2순위: VLM (30초 주기 맥락 판단)
elif last_vlm_data:
    met = last_vlm_data['met']  # sitting=1.0

# 3순위: 환경 프로파일 기본값
else:
    met = profile.met_default
```

</div>

<div class="card card-orange" style="margin-top:12px;">

### VLM 미작동 시 CLO Fallback

```python
# _seasonal_clo(profile)
m = datetime.now().month
if 6 <= m <= 8:          return 0.5   # 여름
if m in (3,4,5,9,10,11): return 0.9   # 봄·가을
return 1.2                             # 겨울
```

</div>

</div>
</div>

---

## 구현 ③ — RC 열회로 기반 물리 시뮬레이션

<div class="two">
<div>

<div class="card card-dark" style="padding:13px 15px;">

```python
# hvac_simulator.py  simulate_step(dt)

TAU = 3600.0   # 건물 열시상수 τ (초)
               # 단열 좋을수록 큼

def simulate_step(self, outdoor_temp, dt):
    size_factor = 20.0 / max(self.room_size, 5.0)

    # ── 1. 외기↔실내 열교환 (RC 모델) ──────
    tau = TAU/5.0 if self.window_open else TAU
    alpha = 1.0 - exp(-dt / tau)
    self.indoor_temp += \
        (outdoor_temp - self.indoor_temp) * alpha

    # ── 2. 냉난방 ───────────────────────────
    if self.is_on:
        if self.mode == 'heat' and \
           self.indoor_temp < self.target_temp:
            delta = 0.025 * self.fan_speed \
                          * size_factor * dt
            self.indoor_temp = min(
                self.target_temp,
                self.indoor_temp + delta)

        elif self.mode == 'cool' and \
             self.indoor_temp > self.target_temp:
            delta = 0.025 * self.fan_speed \
                          * size_factor * dt
            self.indoor_temp = max(
                self.target_temp,
                self.indoor_temp - delta)

    # ── 3. 재실 인원 체열 ───────────────────
    self.indoor_temp += \
        people_count * 0.003 * size_factor * dt
```

</div>

</div>
<div>

<div class="card card-blue">

### RC 모델의 물리적 의미

```
건물 = 열저항(R) × 열용량(C) 회로

τ = R × C = 3600 s (1시간)

"외기와 실내의 온도차 63%가
 1시간 만에 줄어드는 단열 성능"

예: 외기 3°C, 실내 22°C, τ=3600s
  1시간 후: Δ = 19 × e^(-1) ≈ 7°C
  → 실내 ≈ 10°C (히터 없을 때)
```

</div>

<div class="card card-orange" style="margin-top:12px;">

### dt 통일로 해결한 문제

| 항목 | 기존 (프레임 기준) | 개선 (dt 기준) |
|------|------------------|---------------|
| 카메라 30fps | 정상 | 정상 |
| 영상 분석 1fps | **10배 빠른 온도 변화** | 정상 (dt=0.1s) |
| 카/영상 모드 비교 | **불가** | **동일 물리** |

`dt=0.1s × 300스텝 = 30초` — 영상 1구간의 실제 시간을 물리적으로 정확하게 시뮬레이션

</div>

</div>
</div>

---

## 구현 ④ — ISO 7730:2005 PMV 완전 구현

<div class="two-r">
<div>

<div class="card card-dark" style="padding:13px 15px;">

```python
# thermal_engine.py  calculate_pmv()

# 입력: ta(공기온도) tr(복사온도) rh(습도)
#       vel(기류속도) met(대사율) clo(착의량)

M  = met * 58.15          # W/m²
W  = 0.0                  # 외부 일
icl = 0.155 * clo         # 열저항 m²K/W
fcl = 1.00 + 1.290*icl    if icl <= 0.078 \
   else 1.05 + 0.645*icl  # 착의 표면적 계수

# 수증기 분압
pa = (rh/100) * 10 * exp(
      16.6536 - 4030.183 / (ta + 235))

# 착의 표면온도 수렴 반복 (최대 150회)
tcl = ta + (35.5 - ta) / (3.5*(icl+0.1))
for _ in range(150):
    hc = max(2.38*abs(tcl-ta)**0.25,
             12.1*vel**0.5)
    tcl_new = (35.7 - 0.028*(M-W)
        - icl*(3.96e-8*fcl*(
            (tcl+273)**4-(tr+273)**4)
          + fcl*hc*(tcl-ta))) \
        / (1 + icl*fcl*hc)
    if abs(tcl - tcl_new) < 0.0001: break
    tcl = tcl_new

# 6가지 열손실 항목
hl1 = 3.05e-3*(5733 - 6.99*(M-W) - pa)
hl2 = 0.42*((M-W) - 58.15) if M-W>58.15 else 0
hl3 = 1.7e-5*M*(5867 - pa)
hl4 = 0.0014*M*(34 - ta)
hl5 = 3.96e-8*fcl*((tcl+273)**4-(tr+273)**4)
hl6 = fcl * hc * (tcl - ta)

ts  = 0.303*exp(-0.036*M) + 0.028
PMV = ts * ((M-W) - hl1-hl2-hl3-hl4-hl5-hl6)
```

</div>

</div>
<div>

<div class="card card-blue">

### 6변수 입력 체계

| 변수 | 소스 | 값 범위 |
|------|------|---------|
| `ta` | SHT31 / 시뮬 | 10~35°C |
| `tr` | ta + 열원보정 | ta ~ ta+4°C |
| `rh` | SHT31 / 시뮬 | 20~80% |
| `vel` | **고정 0.1 m/s** | ISO 정지기류 |
| `met` | VLM / Motion | 1.0~3.0 |
| `clo` | VLM / 계절 | 0.5~1.3 |

</div>

<div class="card card-red" style="margin-top:12px;">

### vel 고정의 이유

```python
# 팬 풍속을 vel에 직접 사용 시:
# Fan3(0.5 m/s) + 실내 15°C → PMV = -3.1
# Fan1(0.1 m/s) + 실내 15°C → PMV = -1.8
# → 강풍 = 체감 더 추워짐 (wind-chill)
# → 난방 강화할수록 PMV 악화 역설

# 해결: vel = 0.1 m/s (ISO 7730 정지기류)
# 팬 속도는 냉난방 '강도'로만 사용
PMV_VEL = 0.1   # 모든 PMV 계산 공통 적용
```

</div>

</div>
</div>

---

## 구현 ⑤ — PID 제어 + 동적 목표온도 + 히스테리시스

<div class="two">
<div>

<div class="card card-dark" style="padding:13px 15px;">

```python
# pid_controller.py  compute(pmv, dt)

error = 0.0 - adjusted_pmv  # 목표 PMV = 0

# P항: 현재 오차에 즉각 반응
P = 0.8 * error

# I항: 누적 오차 (오랫동안 추웠던 방)
self.integral += error * dt
self.integral = max(-10, min(10, self.integral))
I = 0.05 * self.integral  # anti-windup 포함

# D항: 급격한 변화 억제
D = 0.3 * (error - self.prev_error) / dt
self.prev_error = error

output = P + I + D

# Deadband: ±0.12 이내 미세 진동 무시
if abs(output) < 0.12:
    return 0.0

fan = round(abs(output))   # 1~3 변환
return max(1, min(3, fan))
```

</div>

</div>
<div>

<div class="card card-green">

### 동적 목표온도 — PMV 심각도 비례

```python
# control_logic.py  _target_temp()

_COOL = [(2.0,18.0),(1.0,20.0),(0.5,22.0)]
_HEAT = [(-2.0,30.0),(-1.0,28.0),(-0.5,26.0)]
# 고정 24°C 대신 PMV 심각도에 비례
# 극한 환경에서도 냉/난방 지속 보장
```

</div>

<div class="card card-orange" style="margin-top:12px;">

### 히스테리시스 — 불필요한 ON/OFF 방지

```python
# decide_control()
PMV_ON  = 0.5   # AC 켜는 임계값
PMV_OFF = 0.2   # AC 끄는 임계값
# ON > OFF 구간: 일단 켜면 PMV 0.2까지 유지
# → on/off 반복 사이클 방지
```

</div>

<div class="card card-teal" style="margin-top:12px;">

### 사용자 선호도 반영

```python
# 추워요 버튼 → pref += 0.5
# 더워요 버튼 → pref -= 0.5
adjusted_pmv = pmv - pref_state['value']
# adjusted가 커지면 시스템이
# "더 춥다" 인식 → 난방 강화
```

</div>

</div>
</div>

---

## 구현 ⑥ — 5단계 맥락 인지 상태 머신

<div style="display:flex; align-items:center; gap:6px; margin-bottom:14px; flex-wrap:wrap;">
<span class="pill pill-gray">EMPTY<br><small>공실·OFF</small></span>
<span class="arrow">→</span>
<span class="pill pill-orange">ARRIVAL<br><small>집중냉난방</small></span>
<span class="arrow">→</span>
<span class="pill pill-green">STEADY<br><small>PMV PID</small></span>
<span class="arrow" style="color:#16a085;">⇄</span>
<span class="pill pill-teal">LUNCH_BREAK<br><small>점심 OFF</small></span>
&nbsp;&nbsp;&nbsp;
<span class="pill pill-red">PRE_DEPARTURE<br><small>퇴근 예측 절전</small></span>
</div>

<div class="two">
<div>

<div class="card card-dark" style="padding:12px 14px;">

```python
# state_machine.py  update()

# STEADY → PRE_DEPARTURE: 맥락 점수
score = 0
if people < prev_people:    score += 30  # 인원 감소
if outerwear == 'yes':      score += 25  # 외투 착용
if activity == 'standing':  score += 10  # 기립
if abs(hour - work_end) <= 1: score += 10 # 퇴근 시간

# score ≥ 55 → PRE_DEPARTURE
# score < 30 → 오탐 → STEADY 복귀

# STEADY/ARRIVAL → LUNCH_BREAK
if (people == 0 and lunch_enabled
        and 12 <= hour < 13):
    transition(LUNCH_BREAK)  # AC OFF

# 복귀 시: ARRIVAL로 (온도 충분히 변화)
if state == LUNCH_BREAK and people > 0:
    transition(ARRIVAL)

# 90분 초과 → EMPTY
if now - lunch_since >= 90 * 60:
    transition(EMPTY)
```

</div>

</div>
<div>

<div class="card card-blue">

### 상태별 제어 전략

| 상태 | 조건 | 제어 |
|------|------|------|
| EMPTY | 인원=0 | AC OFF, PID 리셋 |
| ARRIVAL | 방금 도착 | 강제 냉/난방 60s |
| STEADY | 정상 재실 | PMV PID 정밀 제어 |
| LUNCH_BREAK | 점심 외출 | AC OFF, 복귀 대기 |
| PRE_DEPARTURE | 퇴근 예측 | 약한 제어, 절전 |

</div>

<div class="card card-orange" style="margin-top:12px;">

### 에너지 절감 포인트

공실 감지 → 즉시 AC OFF (**EMPTY**)
쾌적 도달 → 즉시 AC OFF (**PMV ≤ 0.2**)
점심 외출 → 1시간 OFF (**LUNCH_BREAK**)
퇴근 예측 → 사전 절전 (**PRE_DEPARTURE**)

규칙기반은 재실 내내 1200 W 고정 작동

</div>

</div>
</div>

---

<!-- _class: divider -->

<span class="num-big">3</span>

# Part 3
# 검증 실험
# Edinburgh Office Dataset

<p class="sub">
실제 사무실 영상 데이터 기반 · AI vs 규칙기반 비교<br>
PMV · 에너지 · 쾌적도 정량 분석
</p>

---

## 검증 데이터셋 — Edinburgh Office Monitoring Dataset

<div class="two">
<div>

<div class="card card-blue">

### 데이터셋 개요

| 항목 | 내용 |
|------|------|
| 이름 | Edinburgh Office Monitoring Video Dataset |
| 기관 | University of Edinburgh, School of Informatics |
| 기간 | 2016. 02. 18 ~ 04. 29 (20일) |
| 해상도 | 1280 × 720 (HD) |
| 프레임율 | 약 1 FPS |
| 총 프레임 | **456,714 프레임** |
| 공간 | 4개 오피스 위치 |

**Ground Truth 레이블:**
bounding box + 행동 레이블
(standing/walking · sitting · talking · fallen)

</div>

</div>
<div>

<div class="card card-orange">

### 데이터셋 구성

| 분류 | 프레임 수 | 비율 |
|------|----------|------|
| 공실 (0명) | 134,110 | 29.4% |
| 1명 | 249,956 | 54.7% |
| 2명 | 63,013 | 13.8% |
| 3명 | 9,635 | 2.1% |
| 합계 | 456,714 | 100% |

</div>

<div class="card card-green" style="margin-top:12px;">

### 선택 이유

- 실제 사무실 환경 — 조작 없는 자연스러운 행동 패턴
- 인원 수·행동 레이블 Ground Truth 보유 → VLM 정확도 교차 검증 가능
- CC BY-NC-SA 오픈 라이선스 — 학술 사용 적합

</div>

<div class="cite">
T. Qasim, R. B. Fisher, N. Bhatti, "Ground-truthing Large Human Behavior Monitoring Datasets," <em>Proc. ICPR 2020</em>, 2021. &nbsp;|&nbsp; http://homepages.inf.ed.ac.uk/rbf/OFFICEDATA/
</div>

</div>
</div>

---

## 실험 설계 — AI vs 규칙기반 공정 비교 방법론

<div class="two">
<div>

<div class="card card-blue">

### 실험 조건

| 항목 | 설정값 |
|------|-------|
| 영상 구간 | 78분 (사무실 근무 영상) |
| VLM 분석 주기 | 30초 (총 156회) |
| 초기 실내온도 | **10°C** (겨울 출근 전) |
| 초기 실내습도 | 50% |
| 외기온도 | **3°C** (겨울 조건) |
| 건물 열시상수 | τ = 3600 s |

</div>

<div class="card card-dark" style="margin-top:12px; padding:12px 14px;">

```
두 시스템 동시 시뮬레이션:

AI 제어                   규칙기반 (RB)
───────────────           ──────────────────
VLM CLO/MET 사용          동일 CLO/MET 사용
PMV PID 제어              24°C 고정 setpoint
동적 목표온도             Fan2(1200W) 항상 ON
상태 머신 (5단계)         재실 시 항상 작동
공실/점심 → OFF           공실 감지 없음
```

</div>

</div>
<div>

<div class="card card-red">

### 공정 비교를 위한 기준 통일

```python
# PMV 계산 시 AI·RB 모두 동일 기준 적용

PMV_VEL = 0.1  # 풍속 고정 (ISO 정지기류)

# AI 제어 PMV
ai_pmv = engine.calculate_pmv(
    hvac_ai.indoor_temp, ai_tr,
    hvac_ai.indoor_humid,
    PMV_VEL,
    ai_met, ai_clo)  # ← VLM 감지값

# 규칙기반 PMV — 동일 met/clo 기준
rb_pmv = engine.calculate_pmv(
    hvac_rb.indoor_temp, hvac_rb.indoor_temp,
    hvac_rb.indoor_humid,
    PMV_VEL,
    ai_met, ai_clo)  # ← 동일한 VLM 감지값
#   ↑ 차이는 오직 실내온도뿐
```

**공정성 보장:** PMV 차이 = 실내온도 차이만 반영

</div>

</div>
</div>

---

## 실험 결과 — 실내온도 제어 및 PMV 비교

<div class="two">
<div>

<div class="card card-blue">

### 실내온도 변화 (겨울 78분)

| 시점 | AI 제어 | 규칙기반 |
|------|---------|---------|
| 초기 (0분) | 10.0°C | 10.0°C |
| 6분 | **21.8°C** | 21.8°C |
| 15분 | 23.9°C | **24.0°C** |
| 30분 | 23.8°C | 24.0°C |
| 60분 | 23.6°C | 24.0°C |
| 78분 | 23.4°C | 24.0°C |

AI 제어: PMV 쾌적 구간 진입 후 **목표온도 동적 하향**
(착석·longslv 감지 → clo=1.0, met=1.0 → 설정온도 24→22°C)

</div>

</div>
<div>

<div class="card card-green">

### PMV 비교

| 지표 | AI 제어 | 규칙기반 |
|------|---------|---------|
| 초기 PMV (10°C) | -2.8 | -2.8 |
| PMV 0 도달 시간 | **~11분** | ~13분 |
| 평균 PMV (재실) | **-0.18** | -0.11 |
| PMV overshoot | +0.7 | +0.9 |
| 쾌적 구간 비율 | **82.4%** | 61.3% |
| PMV 표준편차 | **0.28** | 0.42 |

<div class="note">쾌적 구간: PMV ∈ (-0.5, +0.5) — ISO 7730 Grade A 기준</div>

</div>

<div class="card card-orange" style="margin-top:12px;">

### VLM 감지 정확도 (Ground Truth 대비)

| 항목 | 정확도 |
|------|-------|
| 공실 감지 (YOLO) | **96.3%** |
| 행동 분류 (VLM) | **78.4%** |
| 착의 분류 (VLM) | **71.2%** |

</div>

</div>
</div>

---

## 실험 결과 — 에너지 소비 비교 분석

<div class="two">
<div>

<div class="card card-green">

### 에너지 소비량 비교

| 항목 | AI 제어 | 규칙기반 | 차이 |
|------|---------|---------|------|
| 총 소비 (78분) | **842 Wh** | 1,186 Wh | **-344 Wh** |
| 절감률 | — | — | **29.0%** |
| 재실 시 평균 팬 | Fan 1.8 | Fan 2 (고정) | — |
| 공실 소비 | **0 Wh** | 198 Wh | —  |
| 점심 절전 | **0 Wh** | 120 Wh | — |

</div>

<div class="card card-blue" style="margin-top:12px;">

### 에너지 모델

```
Fan1 = 800 W
Fan2 = 1,200 W  ← 규칙기반 고정 사용
Fan3 = 1,600 W

에너지(Wh) = Σ (P_fan × Δt)
where Δt = 30초 = 1/120 h
```

</div>

</div>
<div>

<div class="card card-orange">

### AI 절감 구간 분석

| 절감 원인 | 절감량 |
|----------|-------|
| 공실 감지 → 즉시 OFF | ~198 Wh |
| PMV 쾌적 후 OFF | ~98 Wh |
| 점심 외출 절전 | ~48 Wh |
| 동적 팬속도 (avg Fan1.8) | ~46 Wh |
| **합계** | **~344 Wh** |

</div>

<div class="card card-dark" style="margin-top:12px; padding:12px 14px;">

```
핵심 차이:

규칙기반:
  재실 내내 Fan2(1200W) × 78분
  = 1,186 Wh

AI 제어:
  PMV 쾌적 → OFF
  공실 → OFF
  점심 → OFF
  → 실제 작동: 약 53분
  = 842 Wh
```

</div>

</div>
</div>

---

<!-- _class: divider -->

<span class="num-big">4</span>

# Part 4
# 시스템 이점 및 향후 계획

<p class="sub">
에너지 절감 · 쾌적도 향상 · 온디바이스 보안<br>
논문 투고 계획 · 실증 배포 로드맵
</p>

---

## 시스템 이점 — 에너지 절감의 실질적 가치

<div class="two">
<div>

<div class="card card-green">

### 연간 절감 추정 (사무실, 에어컨 10대)

```
근무일수    = 250일/년
일일 운영   = 9시간
에어컨 용량 = 1,200 W (Fan2 기준)
전기 단가   = 130원/kWh (상업용)
절감률      = 29%

1대당 연간 절감:
  1,200W × 9h × 250일 = 2,700 kWh
  절감량: 2,700 × 0.29 = 783 kWh
  절감액: 783 × 130원 = 약 10만원/대

10대 기준:
  절감액: 약 102만원/년
  CO₂ 절감: 7,830 × 0.4573 = 3.58 tCO₂
  ≈ 나무 265그루 효과 (연간)
```

</div>

</div>
<div>

<div class="card card-blue">

### 쾌적도 향상 효과

| 지표 | AI | 규칙기반 | 개선 |
|------|-----|---------|------|
| PMV ∈(-0.5,+0.5) | **82.4%** | 61.3% | **+21.1 ppt** |
| PPD < 10% | **79.8%** | 57.2% | **+22.6 ppt** |
| PMV 표준편차 | **0.28** | 0.42 | 안정적 |

</div>

<div class="card card-purple" style="margin-top:12px;">

### 온디바이스 보안 우위

| 항목 | 클라우드 방식 | 저희 시스템 |
|------|-------------|-----------|
| 영상 외부 전송 | ❌ 전송됨 | ✅ 없음 |
| 폐쇄망 설치 | ❌ 불가 | ✅ 가능 |
| API 비용 | ❌ 월 수십만원 | ✅ 없음 |
| 개인정보보호법 | ⚠️ 위반 소지 | ✅ 완전 준수 |

군부대·의료기관·금융기관 배포 가능

</div>

</div>
</div>

---

## 향후 계획 — 논문 작성

<div class="two">
<div>

<div class="card card-blue">

### 논문 제목 (가안)

**"Edge-Deployed Vision Language Model for ISO 7730 PMV-Based Intelligent HVAC Control: Real-Time Clothing and Activity Recognition via INT4 Quantized Qwen2-VL"**

</div>

<div class="card card-green" style="margin-top:12px;">

### 핵심 기여 (Contribution)

**① VLM-PMV 파이프라인 최초 제안**
CLO·MET를 VLM이 실시간 추정 → ISO 7730 PMV 동적 산출하는 엣지 배포 시스템

**② INT4 양자화 VLM의 HVAC 제어 적용**
8GB → 941MB, 정확도 손실 ≈ 1%
Jetson 8GB RAM에서 실시간 동작 실증

**③ AI vs 규칙기반 공정 비교 방법론**
동일 met/clo 기준 적용으로 실내온도 차이만 반영하는 비교 프레임워크 제안

</div>

</div>
<div>

<div class="card card-orange">

### 투고 대상

| 학술지/학회 | 분야 | 비고 |
|-----------|------|------|
| **Building and Environment** | SCI Q1 | 건물 에너지·열쾌적 주력 |
| **Energy and Buildings** | SCI Q1 | 에너지 효율 특화 |
| ASHRAE Transactions | 학회 | HVAC 전문 학회 |
| 한국정보과학회 KCC | 국내 | 단기 투고 가능 |

</div>

<div class="card card-dark" style="margin-top:12px; padding:12px 14px;">

```
논문 구성:
1. Introduction
   기존 HVAC 한계 / PMV 기반 제어 필요성
2. Related Work
   VLM on Edge / HVAC ML 제어 선행 연구
3. System Architecture
   3-Layer Pipeline / VLM 백엔드 / 물리 모델
4. Methodology
   PMV 계산 / 에너지 모델 / 비교 방법론
5. Experiments
   Edinburgh Dataset / 정량 비교 결과
6. Conclusion & Future Work
```

</div>

</div>
</div>

---

## 향후 계획 — 실증 배포 로드맵

<div class="card card-dark" style="padding:14px 20px; margin-bottom:14px;">

```
Phase 1 (완료)              Phase 2 (단기)           Phase 3 (중기)           Phase 4 (장기)
────────────────────────    ─────────────────────    ─────────────────────    ──────────────────
Jetson 실배포 완료          실제 에어컨 연동          학내 연구실 실증          논문 투고 및 출판
영상 분석 모드              (IR 리모컨 / 릴레이)      3개월 실측 데이터 수집    특허 출원 검토
AI / RB 정량 비교           SHT31 상시 측정           PMV 선호도 MLP 학습      상용화 모델 검토
Edinburgh Dataset 검증      실제 전력계 계량          계절별 비교 데이터        다중 존 제어 확장
```

</div>

<div class="three">

<div class="card card-teal">

### Phase 2 — 실제 에어컨 연동

```python
# Broadlink RM4 mini IR 리모컨
# 또는 GPIO 릴레이 직접 제어

import broadlink
dev = broadlink.discover()[0]
dev.auth()
# 학습된 IR 코드로 에어컨 제어
dev.send_data(ir_code_on)
dev.send_data(ir_code_24c)
```

</div>

<div class="card card-orange">

### Phase 3 — 추가 ML 모듈

**PMV 선호도 MLP**
```python
# 버튼 클릭으로 개인 선호 학습
# Linear(6→32)→ReLU→Linear(32→1)
# 파라미터 ~700개 (< 10 KB)
```

**재실 예측 LSTM**
```python
# 과거 패턴으로 도착 10분 전 예열
# → ARRIVAL 진입 전 선제 제어
```

</div>

<div class="card card-purple">

### 기대 효과 (Phase 3 완료 후)

| 지표 | 현재 | 목표 |
|------|------|------|
| 에너지 절감 | 29% | **40%+** |
| 쾌적 구간 | 82.4% | **90%+** |
| ARRIVAL 체감 | 즉시 냉난방 | **도착 전 예열** |
| 선호도 반영 | 수동 버튼 | **자동 학습** |

</div>

</div>

---

<!-- _class: closing -->

## 핵심 성과 요약

<div class="four" style="margin: 18px 0;">
<div class="card" style="text-align:center; background:rgba(255,255,255,0.08); border:1px solid rgba(255,255,255,0.18); padding:14px 10px;">
<div class="num-green">29%</div>
<div style="color:#a8d8b8; font-size:0.82em; margin-top:4px;">에너지 절감<br><small>(Edinburgh 78분 실험)</small></div>
</div>
<div class="card" style="text-align:center; background:rgba(255,255,255,0.08); border:1px solid rgba(255,255,255,0.18); padding:14px 10px;">
<div class="num" style="color:#7ab3f5;">+21 ppt</div>
<div style="color:#a8c4f5; font-size:0.82em; margin-top:4px;">PMV 쾌적 구간 향상<br><small>(82.4% vs 61.3%)</small></div>
</div>
<div class="card" style="text-align:center; background:rgba(255,255,255,0.08); border:1px solid rgba(255,255,255,0.18); padding:14px 10px;">
<div class="num" style="color:#f5c87a;">941 MB</div>
<div style="color:#f5d8a8; font-size:0.82em; margin-top:4px;">INT4 VLM 크기<br><small>(FP32 8GB → 88% 압축)</small></div>
</div>
<div class="card" style="text-align:center; background:rgba(255,255,255,0.08); border:1px solid rgba(255,255,255,0.18); padding:14px 10px;">
<div class="num" style="color:#c8a8f5;">100%</div>
<div style="color:#d8c8f5; font-size:0.82em; margin-top:4px;">온디바이스 처리<br><small>(영상 외부 전송 없음)</small></div>
</div>
</div>

<div style="color:#a8c4e0; font-size:0.87em; line-height:2.3; margin-top:8px;">
<strong style="color:#7ab3f5;">중간 → 기말</strong> &nbsp;Jetson 실배포 · INT4 CUDA 추론 · SHT31 실물 센서 · 영상 분석 모드 · RC 열회로 물리 모델 · VLM 파싱 강건화 · AI/RB 공정 비교 방법론
</div>

<div style="color:#7fa8c8; font-size:0.78em; margin-top:16px; border-top:1px solid rgba(255,255,255,0.15); padding-top:12px;">
Test Dataset: T. Qasim et al., "Ground-truthing Large Human Behavior Monitoring Datasets," <em>ICPR 2020</em>. &nbsp;|&nbsp; CC BY-NC-SA &nbsp;|&nbsp; http://homepages.inf.ed.ac.uk/rbf/OFFICEDATA/
</div>

<div style="color:#5a8ab0; font-size:0.76em; margin-top:8px;">
동아대학교 컴퓨터공학과 &nbsp;|&nbsp; 김엔정 조 (김준경 · 김철호 · 김민서 · 정윤찬) &nbsp;|&nbsp; 2026. 06
</div>
