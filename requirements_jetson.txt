# ─── Jetson Orin Nano Super (JetPack 6.x, Python 3.10, CUDA 12.6) ────────────
#
# 사전 조건:
#   1. JetPack 6.x 설치 완료
#   2. cv2는 시스템 GTK 빌드 사용 (PyPI opencv는 Qt 창 크래시 — CLAUDE.md 참고):
#      ln -sfn /usr/lib/python3/dist-packages/cv2.cpython-310-aarch64-linux-gnu.so \
#          <venv>/lib/python3.10/site-packages/cv2.so
#   3. llama.cpp CUDA 빌드 (~/llama.cpp/build) + GGUF 모델 (~/llama.cpp/models/)
#
# Step 1 — 가상환경 생성
#   python3 -m venv vlm-env --system-site-packages
#   source vlm-env/bin/activate
#
# Step 2 — PyTorch (JetPack 전용 wheel, pip 기본 버전 사용 금지)
#   https://developer.nvidia.com/embedded/downloads 에서 JetPack 버전에 맞는
#   torch/torchvision wheel 설치
#
# Step 3 — 이 파일로 나머지 설치
#   pip install -r requirements_jetson.txt
#
# ─────────────────────────────────────────────────────────────────────────────

# VLM (HF 폴백 경로용 — 주 추론은 llama.cpp GGUF)
transformers>=5.12.1
qwen-vl-utils
accelerate

# 이미지/영상
pillow

# 데이터
pandas
numpy
matplotlib

# 통신 (날씨 API + llama-server HTTP)
requests
python-dotenv

# YOLO (인원 감지)
ultralytics

# YouTube 영상 분석 모드
yt-dlp

# SHT31 I2C 온습도 센서
smbus2

# ── 주의 ──────────────────────────────────────────────────────────────────────
# opencv-python 은 시스템 GTK cv2와 충돌하므로 pip 설치 금지 (위 Step 사전조건 2).
# torch/torchvision 은 JetPack 전용 wheel로만 설치.
